@using Microsoft.AspNetCore.Identity
@inject UserManager<AppUser> UserManager

<ul class="navbar-nav">
    @if (User.IsInRole("Admin") || User.IsInRole("Approver"))
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/AppUsers">Users</a>
        </li>
    }
</ul>